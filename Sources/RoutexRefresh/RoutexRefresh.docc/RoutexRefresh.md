# ``RoutexRefresh``

Non-interactive refresh client for the YAXI Open Banking services.

## Overview

``RoutexRefreshClient`` refreshes accounts, balances, and transactions for a connection whose consent an earlier interactive flow established, without prompting the user.

The typical caller is a backend scheduling recurring refreshes.
