# ``RoutexModels``

Domain models for the YAXI Open Banking services.

## Overview

The request and response types shared by the clients: typed tickets, `Response` with its interrupt payloads, the per-service result models, account filters, and the error catalog.

`RoutexClient` and `RoutexRefresh` re-export this module; import it directly only where neither client is needed.
